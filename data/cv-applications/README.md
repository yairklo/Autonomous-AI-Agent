# CV applications queue

# Draft packages from submit_whatsapp_job_cv land here as *.json + *-cover.txt + *-cv.*
# Live WhatsApp send is out of scope; open mailto: from the draft or send manually.
#
# Configure profile via CV_PROFILE_PATH (default: data/cv-profile.json).
# See fixtures/cv/profile.json for an example.
